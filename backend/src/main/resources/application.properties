# --- Database Configuration (PostgreSQL Example) ---
spring.datasource.url=jdbc:postgresql://localhost:5433/cibf_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA/Hibernate Configuration ---
# Create or update tables automatically (great for development)
spring.jpa.hibernate.ddl-auto=update 
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- JWT Configuration (Needed for JwtTokenProvider) ---
app.jwt-secret=RkFVTFRfMjAyNV9DTEJIX1JFU19TWVNURU1fUFJPSkVDVA==
app.jwt-expiration-milliseconds=86400000 # 24 hours
