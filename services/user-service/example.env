# ----------------------------------------------------
# STANDARD VARIABLES (USED BY application.properties)
# ----------------------------------------------------

# JWT Configuration (Secret for signing tokens)
JWT_SECRET=JE6WwDKZ8uMOOwt+XnkQTPvb2sj0BD58Kc1WwxTHfVaB7otOwANuKKhbGPvKjA6hDyo0y7qls24Irt6rHLINEA==

# Database Host, Port, Name (Used to form the spring.datasource.url)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cibf_db
DB_USERNAME=postgres
DB_PASSWORD=postgres

# ----------------------------------------------------
# UNUSED/REDUNDANT VARIABLES (Optional to keep for other services)
# ----------------------------------------------------

# Note: The AUTH_DB_URL, STALL_DB_URL, etc., are redundant 
# if your individual service's application.properties handles
# the currentSchema setting, as seen in your auth-service config.

# AUTH_DB_URL=jdbc:postgresql://localhost:5432/cibf_db?currentSchema=auth_schema
# STALL_DB_URL=jdbc:postgresql://localhost:5432/cibf_db?currentSchema=stall_schema
# RESERVATION_DB_URL=jdbc:postgresql://localhost:5432/cibf_db?currentSchema=reservation_schema
# USER_DB_URL=jdbc:postgresql://localhost:5432/cibf_db?currentSchema=user_schema

AWS_REGION=us-west-2